name: mnist-transformer
channels:
  - pytorch
  - defaults
  - conda-forge
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - tqdm
  - wandb
prefix: /opt/anaconda3/envs/mnist-transformer
